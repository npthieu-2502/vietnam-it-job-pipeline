job_market_profile:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      user: de_user
      password: de_password
      port: 5432
      dbname: job_market
      schema: public
      threads: 4
